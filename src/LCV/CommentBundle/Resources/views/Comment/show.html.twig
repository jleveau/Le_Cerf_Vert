 
 
 {% for comment in list_comment %}
    <div class="media">
    
	   <a class="pull-left" href="#">
	    {% if comment.author.avatar %}
		   {% thumbnail comment.author.avatar, 'small'%} 
		{% endif %}
	   </a>
	   
   		<div class="media-body">
   		<h4 class="media-heading">{{comment.author}}  <small> - Le {{comment.date|date("m/d/Y \à H:m:s")}}</small></h4>
   		{{ comment.content}}
   		   		</div>
   		
   		
    </div>

  {% endfor %}
  